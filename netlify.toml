[build]
  base = "apps/web"
  command = "cd ../.. && bun install && bun run build --filter=@apimon/web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  BUN_VERSION = "1.2.15"

[[plugins]]
  package = "@netlify/plugin-nextjs"
