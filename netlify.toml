[build]
  base = "apps/web"
  command = "cd ../.. && bun install && bun run build --filter=@apimon/web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
